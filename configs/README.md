# Configs

Shared configuration for the project lives here (lint defaults, later data-source lists and eval settings).

`dev-requirements.txt` is the dependency set CI audits with pip-audit.

This directory is not a dataset. Do not commit corpora, weights, or scrapes.
